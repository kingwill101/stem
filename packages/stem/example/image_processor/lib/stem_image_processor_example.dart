/// Image thumbnail generation example using Stem task queue.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to enqueue and process thumbnail generation jobs.
library;

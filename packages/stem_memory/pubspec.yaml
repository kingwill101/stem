name: stem_memory
description: In-memory broker, backend, workflow, and scheduler adapters for Stem.
version: 0.3.0
repository: https://github.com/kingwill101/stem
resolution: workspace
environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  stem: ">=0.5.0 <1.0.0"

dev_dependencies:
  coverage: ^1.15.0
  stem_adapter_tests: ">=0.3.0 <1.0.0"
  test: ^1.31.2
  very_good_analysis: ^10.0.0

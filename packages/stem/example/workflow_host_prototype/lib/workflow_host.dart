/// Compatibility barrel for the historical example package name.
///
/// The implementation lives in the supported `package:stem` host. This file
/// intentionally contains no wrapper types or alternate lifecycle.
library;

export 'package:stem/stem.dart';

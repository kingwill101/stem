/// Worker using Redis broker and Postgres result backend.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the Redis/Postgres worker process.
library;

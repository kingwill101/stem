name: stem_cli
description: Command-line tooling for Stem that orchestrates Redis and Postgres adapters.
version: 0.1.0-alpha.4
repository: https://github.com/kingwill101/stem
resolution: workspace
environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  artisanal: ^0.1.0-dev+4
  stem: ^0.1.0-alpha.4
  stem_redis: ^0.1.0-alpha.4
  stem_postgres: ^0.1.0-alpha.4
  stem_sqlite: ^0.1.0-alpha.4
  redis: ^4.0.0
  postgres: ^3.5.9
  async: ^2.13.0
  collection: ^1.18.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

false_secrets:
  - docker/testing/postgres/certs/server.key

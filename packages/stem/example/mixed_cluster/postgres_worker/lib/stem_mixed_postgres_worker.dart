/// Worker that consumes from Postgres broker and stores results in Postgres.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the Postgres-backed worker in the
/// mixed-cluster example.
library;

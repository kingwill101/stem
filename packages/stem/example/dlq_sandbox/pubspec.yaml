name: stem_dlq_sandbox
description: Demonstrates Stem dead-letter queue inspection and replay workflows.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../..

dev_dependencies:
  lints: ^6.0.0

/// Metadata key storing the result encoder identifier.
const String stemResultEncoderMetaKey = '__stemResultEncoder';

/// Metadata key storing the args encoder identifier.
const String stemArgsEncoderMetaKey = '__stemArgsEncoder';

/// Header key storing the args encoder identifier.
const String stemArgsEncoderHeader = 'stem-args-encoder';

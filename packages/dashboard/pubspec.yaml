name: stem_dashboard
description: Hotwire + Routed dashboard for the Stem runtime.
version: 0.1.0
publish_to: "none"
resolution: workspace
environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  intl: ^0.20.2
  meta: ^1.17.0
  drift: ^2.15.0
  routed: ^0.1.0
  routed_hotwire: ^0.1.0
  stem: 
  stem_sqlite:
    path: ../stem_sqlite

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
  property_testing: ^0.1.0
  routed_testing: ^0.1.0
  server_testing: ^0.1.0

dependency_overrides:
  routed: ^0.1.0
  routed_hotwire:
    git:
      url: https://github.com/kingwill101/routed.git
      path: packages/routed_hotwire
  server_testing: ^0.1.0
  routed_testing: ^0.1.0
  property_testing: ^0.1.0
  redis: ^4.0.0
  contextual: ^2.0.0

/// Worker that consumes from Redis and stores results in Redis.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the Redis-backed worker in the
/// mixed-cluster example.
library;

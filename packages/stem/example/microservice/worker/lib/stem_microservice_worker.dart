/// Dedicated worker process consuming Stem tasks via Redis.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the worker process for the microservice
/// example.
library;

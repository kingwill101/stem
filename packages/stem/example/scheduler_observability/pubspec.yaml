name: stem_scheduler_observability
description: Demonstrates Beat scheduler drift metrics and schedule signals.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../..
  stem_redis:
    path: ../../../stem_redis

dev_dependencies:
  lints: ^6.0.0

dependency_overrides:
  stem:
    path: ../..

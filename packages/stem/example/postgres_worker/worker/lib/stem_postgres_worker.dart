/// Worker process backed by Postgres broker and result backend.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the Postgres-backed worker process.
library;

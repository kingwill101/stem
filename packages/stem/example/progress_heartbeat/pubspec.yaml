name: stem_progress_heartbeat
description: Demonstrates task progress and heartbeat reporting patterns.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../..
  stem_redis:
    path: ../../../stem_redis

dev_dependencies:
  lints: ^6.0.0

dependency_overrides:
  stem:
    path: ../..

name: stem_signals_demo
description: Demonstrates Stem signal hooks with a worker and producer pair.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../..
  stem_redis:
    path: ../../../stem_redis

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

services:
  postgres:
    image: postgres:14
    restart: unless-stopped
    environment:
      POSTGRES_DB: stem_demo
      POSTGRES_USER: stem
      POSTGRES_PASSWORD: stem
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stem -d stem_demo"]
      interval: 5s
      timeout: 3s
      retries: 5

  enqueuer:
    build:
      context: ../..
      dockerfile: example/postgres_worker/enqueuer/Dockerfile
      target: dev
    env_file:
      - .env.example
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${PORT:-8081}:8081"
    restart: unless-stopped

  worker:
    build:
      context: ../..
      dockerfile: example/postgres_worker/worker/Dockerfile
      target: dev
    env_file:
      - .env.example
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

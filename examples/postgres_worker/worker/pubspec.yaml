name: stem_postgres_worker
publish_to: 'none'
description: Worker process backed by Postgres broker and result backend.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../../..

dev_dependencies:
  lints: ^6.0.0

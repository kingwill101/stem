services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  worker:
    build:
      context: ../..
      dockerfile: examples/signals_demo/Dockerfile
    environment:
      STEM_BROKER_URL: redis://redis:6379/0
      WORKER_NAME: signals-demo-worker
    depends_on:
      - redis
    command: ["dart", "bin/worker.dart"]

  producer:
    build:
      context: ../..
      dockerfile: examples/signals_demo/Dockerfile
    environment:
      STEM_BROKER_URL: redis://redis:6379/0
    depends_on:
      - redis
    command: ["dart", "bin/producer.dart"]

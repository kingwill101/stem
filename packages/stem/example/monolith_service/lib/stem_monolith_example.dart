/// Simple monolithic Stem deployment with in-memory adapters.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to launch the monolithic example service.
library;

/// Beat scheduler for the Stem microservice example.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to publish scheduled jobs for the microservice
/// example.
library;

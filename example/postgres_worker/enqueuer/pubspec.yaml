name: stem_postgres_enqueuer
publish_to: 'none'
description: Example enqueuer that uses Postgres for broker and result backend.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  stem:
    path: ../../..

dev_dependencies:
  lints: ^6.0.0

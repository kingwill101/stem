/// Minimal example streaming Stem metrics to an OpenTelemetry collector.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the metrics example components.
library;

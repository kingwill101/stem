name: stem_postgres
description: Postgres broker, result backend, and scheduler utilities for Stem.
version: 0.3.0
repository: https://github.com/kingwill101/stem
resolution: workspace
environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  artisanal: ^0.5.0
  contextual: ^2.2.0
  ormed: ">=0.4.0 <1.0.0"
  ormed_postgres: ">=0.3.1 <1.0.0"
  stem: ">=0.5.0 <1.0.0"
  uuid: ^4.5.2

dev_dependencies:
  build_runner: ^2.10.5
  coverage: ^1.15.0
  stem_adapter_tests: ">=0.3.0 <1.0.0"
  test: ^1.31.2
  very_good_analysis: ^10.0.0

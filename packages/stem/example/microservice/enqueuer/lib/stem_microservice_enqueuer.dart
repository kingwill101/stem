/// HTTP API that enqueues Stem tasks using Redis.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to start the enqueue API for the microservice
/// example.
library;

/// Example enqueuer that uses Postgres for broker and result backend.
///
/// This package is an executable example rather than a reusable library. Run
/// the scripts under `bin/` to publish tasks into the Postgres worker example.
library;
